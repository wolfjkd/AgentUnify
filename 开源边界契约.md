# 开源边界契约

> 本仓是**框架仓**，不是**实例仓**。
> 这份文件把「开发自用」与「开源」之间的那条线一次画清 —— 线画定之后，两边零重叠，不必再逐行犹豫要不要脱敏。

## 一、一条线

> **只开机制，不开实例。**

- **机制** = 换个陌生人拿到也能直接跑起来的东西（他是谁、在哪台机器、做什么业务，都不影响）。
- **实例** = 只有本机这个人才能跑起来的东西（人设、领域规则取值、记忆、真实路径、私有清单）。

## 二、判据：文件夹即口径（v0.3.1 起）

不再逐个文件做「敏感 / 不敏感」的个案裁量 —— 那种裁量本身就是漂移之源。
真源里**按文件夹切分**，机械可判、零歧义：

| 层 | 范围 | 能否开源 |
|---|---|---|
| **框架层（方法论）** | 真源根级的 3 篇：`ai_rules_architecture.md` · `ai_rules_collaboration.md` · `AI-Rules镜像同步规范.md` | ✅ 脱敏后可开源 |
| **实例层（私人）** | `1-人设/` `2-规则/` `3-记忆/` `4-索引/` 四个目录内**一切** md | ⛔ 禁止开源 |

> 判据一句话：**文件名落在上述 4 个目录内 → 私人；落在真源根级 → 方法论。**

## 三、什么进仓（机制）

| 类别 | 形态 |
|---|---|
| 方法论文档 | 真源根级 3 篇（发布器脱敏后生成，见 §五） |
| 本仓自有文档 | `README.md` · `AGENTS.md` · `CHANGELOG.md` · 本契约 |
| 脚本 | `_scripts/*.py`（脱敏骨架：逻辑完整、措辞中立） |
| 配置样板 | `config.example.json`（占位路径） |
| 空槽位说明 | `1-人设/` `2-规则/` `3-记忆/` `4-索引/` 只放「使用者自建」的 README |

## 四、什么永不进仓（实例）

| 类别 | 唯一去处 |
|---|---|
| 人设实际内容 | 使用者本地真源 |
| 领域规则取值 | 使用者本地真源 |
| 记忆 / 工作日志 | 使用者本地真源 |
| 索引产物 | 使用者本地真源（脚本生成） |
| 真实路径 / 机器名 / 账号 | 使用者本地 `config.json`（已 gitignore） |
| 私有项目清单 / 业务标的 | 使用者本地真源 |

> 本仓的 `1-人设/` `2-规则/` `3-记忆/` `4-索引/` 永远是空槽位 —— 不是忘了填，是**设计上不填**。
> 使用者克隆后按 README 自建自己的内容。

## 五、两类产物：都走同一条单向流水线（维护者约定）

| 产物 | 私有源 | 本仓落点 | 是否入库 |
|---|---|---|---|
| 脚本 | `_local/_scripts/*.py` | `_scripts/*.py` | ✅ |
| 方法论 md | 真源根级 3 篇 | 仓根同名 | ✅ |
| 运行体 / 日志 / 配置 | `_local/` | — | ❌ |

- 方向**单向**：真源 → 脱敏 → 覆盖本仓，永不反向。
- 发布器 `_local/publish.py`（不入库）做机械转换：**措辞脱敏** + **路径翻译**（真源机器路径 → 占位符、私有运行布局 → 本仓等价布局）。
- 守卫分两级：
  - **硬拦** —— 命中真实账号 / 机器路径 / 私有项目名 → 拒绝写入并报错；
  - **软替换** —— 人名、项目名、路径等机械替换，不拦。
- 改了真源方法论 md 或私有脚本 → **先发布、再提交**，两边永远同步。

> ⚠️ **方法论 md 是发布产物，不是镜像副本** —— 因此**不对它做「漂移比对」**：
> 真源改了，重新发布覆盖即可，不必逐行 diff 判断「谁新谁对」。
> （过去靠人工重写这几份文档，正是历次 stale 声明的来源。）

## 六、提交前自检（三条）

1. 发布检查：`publish.py check` 必须「无脱敏残留」。
2. `git status` 不得出现 `_local/`、`config.json`、`_archive/`、运行日志。
3. 动过脚本或方法论 md → 先跑发布同步，再提交。

## 七、判定口诀

> 拿不准某个文件该不该进仓，就问一句：
> **「换个陌生人拿到它，需要改哪些地方才能跑起来？」**
>
> - 需要改的 → 那是**实例**，挪出仓（或换成占位符）
> - 不用改的 → 那是**机制**，可以进仓
>
> 放在本体系里，这条口诀与 §二 的「文件夹即口径」等价：
> 真源根级 3 篇 = 机制；4 个私人目录 = 实例。

---

*本契约属于「机制」，随框架一起开源。修订即改本文件，不需要脱敏流程。*
