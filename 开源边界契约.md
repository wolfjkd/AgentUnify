# 开源边界契约

> 本仓是**框架仓**，不是**实例仓**。
> 这份文件把「开发自用」与「开源」之间的那条线一次画清 —— 线画定之后，两边零重叠，不必再逐行犹豫要不要脱敏。

## 一、一条线

> **只开机制，不开实例。**
> 凡提到具体的人 / 组织 / 项目 / 机器路径 / 业务标的的内容，一律不进本仓。

- **机制** = 换个陌生人拿到也能直接跑起来的东西（他是谁、在哪台机器、做什么业务，都不影响）。
- **实例** = 只有本机这个人才能跑起来的东西（人设、领域取值、真实路径、私有清单）。

## 二、什么进仓（机制）

| 类别 | 形态 |
|---|---|
| 机制文档 | 协作协议、架构决策、同步规范、本契约 |
| 脚本 | `_scripts/*.py`（脱敏骨架：逻辑完整、措辞中立） |
| 配置样板 | `config.example.json`（占位路径） |
| 空槽位说明 | `1-人设/` `3-记忆/` `4-索引/` 只放「使用者自建」的 README |

## 三、什么永不进仓（实例）

| 类别 | 唯一去处 |
|---|---|
| 人设实际内容 | 使用者本地真源 |
| 领域规则取值 | 使用者本地真源 |
| 记忆 / 工作日志 | 使用者本地真源 |
| 真实路径 / 机器名 / 账号 | 使用者本地 `config.json`（已 gitignore） |
| 私有项目清单 / 业务标的 | 使用者本地真源 |

> 本仓的 `1-人设/` `3-记忆/` 永远是空槽位 —— 不是忘了填，是**设计上不填**。
> 使用者克隆后按 README 自建自己的内容。

## 四、脚本：两份、单向（维护者约定）

| 位置 | 角色 | 是否入库 |
|---|---|---|
| 私有工作区 `_local/_scripts/` | 运行版（本机在用，措辞自然） | ❌ |
| 本仓 `_scripts/` | 开源脱敏版 | ✅ |

- 两份是**同构副本**：逻辑完全相同，唯一差异是措辞脱敏。
- 方向**单向**：私有 → 开源，永不反向。
- 发布器 `_local/publish.py`（不入库）做机械转换 + 守卫扫描：命中真实路径或私人关键词即**拒绝写入**。
- 改了私有脚本 → **先发布、再提交**，两份永远同步。

## 五、提交前自检（三条）

1. 发布检查：守卫扫描必须「无脱敏残留」。
2. `git status` 不得出现 `_local/`、`config.json`、`_archive/`、运行日志。
3. 动过脚本 → 先跑发布同步，再提交。

## 六、判定口诀

> 拿不准某个文件该不该进仓，就问一句：
> **「换个陌生人拿到它，需要改哪些地方才能跑起来？」**
>
> - 需要改的 → 那是**实例**，挪出仓（或换成占位符）
> - 不用改的 → 那是**机制**，可以进仓

---

*本契约属于「机制」，随框架一起开源。修订即改本文件，不需要脱敏流程。*
