# AI-Rules 多智能体协作规则（核心 · 所有 AI 必读）

> 本规则是「AI-Rules 统一规则体系」的灵魂，优先级最高。任何接入本体系的 AI 智能体（WorkBuddy、Trae、豆包、ZCode 等），在处理「人设/规则/记忆」文件时，**必须遵守本规则**。

## 一、体系定位（谁是真源）

- **Obsidian `你的真源目录\` 是唯一真源**，所有人设、规则、索引都在这里。
- 各 AI 工具的本地文件（各工具自己的本地目录）是**本地副本/缓存**，由真源「下发」而来。
- **Owner 本人不手动编辑任何 md 文件**，所有编辑都交由 AI 完成；Owner 是**唯一 Owner**，负责最终裁决。

### 1.1 文件层级（AGENTS.md 是最高统领）

| 层级 | 文件 | 作用 |
|---|---|---|
| **最高统领** | `AGENTS.md` | WorkBuddy 的规则索引 + 行为规范，**每次对话必加载**，指向所有规则子文档 |
| **体系宪法** | `ai_rules_collaboration.md` | 跨工具通用协作规则（本文档），所有 AI 必读 |
| **规则细节** | `2-规则/*.md` | 单条规则，懒加载（命中触发词才读） |
| **人设** | `1-人设/*.md` | AI 是谁 |
| **记忆** | `3-记忆/*.md` | 定期提炼汇总 |
| **索引** | `4-索引/*.md` | 索引文件 |

> ⚠️ **铁律**：每次增删改 `2-规则/` 下的规则文件时，**必须同步检查 AGENTS.md 是否需要修改**——因为 AGENTS.md 是所有规则文件的索引，规则变了索引也要跟着变。
> 🔍 **防错校验**：增删改规则后，**必须跑一次 `py mirror.py check`** 校验「AGENTS.md 索引 ↔ 规则文件」是否一一对应。脚本会抓出「孤儿规则（有文件没引用）」和「僵尸引用（有引用没文件）」，两者都应为 0。

### 1.2 命名与编码约定（Owner 偏好）

> Owner 原则：**在不影响代码执行、不碰特殊编码雷区的前提下，尽量用中文（或加中文注释），方便阅读。**

| 场景 | 能否用中文 | 说明 |
|---|---|---|
| md 文件内容、文件夹名、文件名 | ✅ 随便用 | Obsidian/文件系统都支持 UTF-8 |
| Python 脚本里的注释（`#` 后） | ✅ 能用 | 注释不影响执行 |
| Python 脚本的 `print` 输出 | ⚠️ 看环境 | cmd 双击时可能乱码（详见 bat 铁律） |
| `.bat` 文件内容 | ❌ 尽量别用 | cmd 用 GBK 解析，中文易乱码报错 |

- **文件夹/文件名**：优先中文（如「人设」「规则」「记忆」「索引」），数字序号用 `1/2/3/4`（不用 `00/10/20/30` 这种冗余前导零）。
- **Python 注释**：用中文注释，方便 Owner 阅读。
- **例外**：`.bat` 内容、以及「会输出到 Windows cmd 屏幕」的 `print`，用英文（避免乱码）。

## 二、三类文件，三种治理（务必分清）

| 类型 | 内容 | 真源 | 是否跨工具共享 | 治理方式 |
|---|---|---|---|---|
| **人设/规则** | SOUL、USER、IDENTITY、规则 | Obsidian | ✅ 共享 | **高频**：AI 改 → 写真源 → 下发 |
| **记忆** | MEMORY 及各 AI 记忆 | Obsidian | ✅ 共享 | **定期提炼汇总**（见第八章），非实时同步 |
| **索引** | 索引文件 | Obsidian | ✅ 共享 | 脚本/AI 扫描生成 |

## 三、编辑铁律（每次动手前必读）

1. **先读后改**：编辑任何文件前，先读 Obsidian 真源的**当前版本**，在此基础上改，不做无头编辑。
2. **先对比再增删改**：不要「同步」「覆盖」。每次改动前先 `compare`（对比），明确「新增/修改/删除」哪几处，再执行。
3. **绝不盲目覆盖**：发现「两边都改过、且内容不同」→ **立即停手，生成差异报告交 Owner 裁决**，自己一个字都不动。
4. **改完写回真源**：编辑完成后，写回 Obsidian 真源，然后「下发」到各工具本地副本，保持一致。

## 四、冲突裁决（铁律，不可违背）

- **Owner 是唯一 Owner，唯一裁决者。**
- 任何 AI 的增删改都是「建议」，不是「决定」。
- 当两个 AI 对同一处有不同改动时：**报 Owner 裁决**（不自动取谁新、不自动取谁对）。
- AI 之间**平级**，可互相指出疑似错误（附理由），但**不得擅自回改对方的内容**。

## 五、操作流程（标准动作）

```
1. 读 Obsidian 真源当前版本
2. compare 对比（自己本地 vs 真源）
3. 判断差异：
   - 只有一边有改动 → 按方向执行增删改
   - 两边都有改动且不同 → 报 Owner 裁决
4. 执行增删改（新增/更新/删除）
5. 写回 Obsidian 真源
6. 下发到各工具本地副本
7. 留痕（日志）
```

## 六、脚本的定位（工具，不是「大脑」）

- `AI-Rules/_scripts/` 下的脚本负责：**compare（比对）+ check（索引校验）+ backup（备份）+ sync（机械下发）**。
- **sync 只做「确定安全」的下发**：`sync --apply` 仅下发「仅真源有」（真源新增、副本缺失，无裁决）；「内容不同」「仅副本有」一律停下，交 Owner 裁决。
- **脚本不裁决**：冲突以哪边为准永远由 Owner 拍板；归源、内容合并等「内容编辑」由 AI 按本规则执行。

## 七、各 AI 的职责边界

| 角色 | 职责 |
|---|---|
| **Owner** | 唯一 Owner，裁决冲突，不动手编辑 |
| **WorkBuddy** | 主要执行者，直接读写真源 + 下发副本 |
| **其他 AI（Trae/豆包/ZCode）** | 读真源工作，改动向真源汇报，冲突报 Owner |
| **脚本** | 机械 compare + 留痕，不判断不改内容 |

## 八、记忆提炼汇总（重要，替代「各写各的」）

> 记忆**不搞实时同步**（避免天天冲突），而是**定期提炼汇总**，最终也归一到 Obsidian 一份。

### 8.1 核心原则

- **提炼谁的记忆，谁当提炼者**：每个 AI 只负责提炼「自己的记忆文件」，不越权解读其他 AI 的记忆。
- **触发方式**：**手动触发**（Owner 说「提炼记忆」/「汇总记忆」时执行）。当前各 AI 使用不频繁，暂不挂自动定时。
- **节奏**：平时各 AI 各自积累记忆；Owner 要求时，各自提炼、汇总到 Obsidian 真源。

### 8.2 提炼五步流程

```
1. 触发：Owner 说「提炼记忆」
2. 提炼：该 AI 读自己的记忆文件，去重、合并同类、整理成结构化草稿
3. 裁决：草稿中的「矛盾项/不确定项」报 Owner 拍板，无矛盾项直接采纳
4. 写回：定稿写入 Obsidian 3-记忆/ 真源
5. 下发：下发回各工具本地副本，保持一致
```

### 8.3 矛盾项处理

- 提炼时若发现「两个 AI 记了矛盾的事」（如一个记「删文件用 rm」、一个记「删文件走回收站」），**不得擅自取舍**，须报 Owner 裁决。
- Owner 拍板后，以 Owner 结论为准，写入真源并下发。

### 8.4 记忆文件位置约定

- Obsidian 真源：`你的真源目录\3-记忆\`
- 建议按工具分文件，如 `3-记忆/WorkBuddy记忆.md`、`3-记忆/Trae记忆.md` 等，避免互相覆盖。
