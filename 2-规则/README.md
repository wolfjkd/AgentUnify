# 规则目录

这里存放**领域规则**（单条规则，懒加载：命中触发词才读）。

> 例：版本管理、技术踩坑、目录约定、业务领域规则……
> 放进来后，在真源根级 `AGENTS.md` 的懒加载总表里登记一行（规则 ↔ 索引必须一一对应）。

**本仓（框架仓）不携带任何规则内容** —— 规则取值属「私人实例」，请在你自己的真源里自建，
**勿提交个人私密内容**。本目录在本仓只是一处空槽位。

> 方法与框架无关的**通用方法论**不放这里，它属真源根级（见 `开源边界契约.md` §二）。
