# AI-Rules 架构决策文档（现行架构 + 演进史）

> 最后更新：2026-09-13（v0.2.0）
> 状态：**4 工具标准化接入完成**：WorkBuddy（1:1 翻译）/ Trae Work（1:1 翻译）/ ZCode（inbox 直拷）/ 豆包（手动，无本地目录）
>
> ⚠️ **本文档记录「架构怎么设计的」和「怎么一步步演进到今天」。** 现行操作规则、铁律、流程，请以 **`ai_rules_collaboration.md`** 为准（那才是所有 AI 必读的现行规则）。

---

## 一、目标

让本机的多个 AI 智能体工具（WorkBuddy、Trae、豆包工作、ZCode 等）共享**一套一致的人设、规则、记忆**，只维护一份真源，避免各自为政、人格分裂、规则漂移。

## 二、现行架构：星型拓扑 + 错峰收敛

```
                Owner（唯一裁决者）
                     ▲ 差异汇总，Owner 拍板
                     │
        ┌────────────┼────────────┐
        │            │            │
   WorkBuddy      Trae        豆包/ZCode   （平级，各自写本地，互审建议）
        │            │            │
        └────────────┼────────────┘
                     ▼
        Obsidian 真源（你的真源目录\）
        （脚本做 compare/check/backup/sync 机械下发，不裁决）
```

- **唯一真源**：Obsidian `AI-Rules\`（Owner 不动手编辑，全交由 AI 编辑）
- **本地副本**：各工具自己的固定路径（各工具自己的本地目录），由真源「下发」而来
- **裁决中心**：Owner 本人（唯一裁决者）

## 三、三条铁律（不可违背）

1. **Owner = 唯一裁决者**：任何对规则/人设/记忆的增删改，最终都要 Owner 批准。没有 AI 能绕过 Owner 改规则。
2. **脚本 = 机械工具**：只做 compare（比对）/ check（索引校验）/ backup（备份）/ sync（机械下发）。**不裁决**——冲突以哪边为准永远 Owner 拍板；sync 只下发「仅真源有」这类确定安全的项，绝不覆盖「内容不同 / 仅副本有」。
3. **AI 互审 = 语义兜底**：各 AI 平级。对比真源时，若发现其他 AI 的改动疑似错误，可指出差异并附理由，但**不得擅自回改**，须交 Owner 审核。

## 四、三类文件，三种治理（现行）

| 类型 | 治理方式 | 频率 |
|---|---|---|
| **人设/规则** | AI 改 → 写真源 → 下发 | 高频（每次改动） |
| **记忆** | **定期提炼汇总**（详见协作规则第八章） | 手动触发 |
| **索引** | 脚本/AI 扫描生成 | 低频 |

## 五、目录结构（现行）

```
你的真源目录\
├── AGENTS.md                          ← 最高统领（规则索引 + 行为规范）
├── ai_rules_collaboration.md         ← 协作规则（权威，所有 AI 必读）
├── ai_rules_architecture.md          ← 本文档（架构背景 + 演进史）
├── 1-人设\                            ← 人设（SOUL/USER/IDENTITY，用户自建）
├── 2-规则\
│   └── AI-Rules镜像同步规范.md        ← 镜像同步规范（AgentUnify 强相关）
├── 3-记忆\                            ← 记忆（定期提炼，用户自建）
├── 4-索引\                            ← 索引（脚本/AI 扫描生成）
├── _scripts\
│   └── mirror.py                      ← compare + check + backup + sync
└── _archive\                          ← 备份（不入库）
```

## 六、冲突裁决规则（现行）

- **两边都改过且内容不同 → Owner 人工裁决**（脚本检测到差异即报告，绝不自动覆盖）。
- 任何 AI 的增删改都是「建议」，最终以 Owner 批准为准。

## 七、实施分期

- **第一期**（✅ v0.1.0）：WorkBuddy ↔ Obsidian，打通「compare 对比 + 冲突报告 + 留痕」闭环。
- **第二期**（✅ v0.2.0）：Trae 全量接入（路径中立化 + 归一化比对 + sync 机械下发）；ZCode inbox 模式接入（取代旧 N:1 拼接方案）；双向遍历加固副本侧孤儿扫描。
- **第三期**（待启动）：豆包接入（无本地目录，手动路径待定）。

---

## 附录：架构演进史（记录关键决策，供后来 AI 理解「为什么这么设计」）

### 演进 1：从「WorkBuddy 是主源」到「Obsidian 是唯一真源」

最初设想「WorkBuddy 的核心文件为蓝本，Obsidian 作为镜像」。后 Owner 明确：**Obsidian 才是所有 AI 的镜像源/真源，WorkBuddy 不再是真源**，降级为「本地副本」。

### 演进 2：从「脚本自动同步」到「AI 编辑 + 脚本降级」

最初脚本带 `sync`（自动 apply，自动改文件）。后 Owner 指出「同步=覆盖」用词不当，应「先对比再增删改」；且「脚本有点多余，不如固化为人格提示词」。最终：**增删改由 AI 按协作规则执行，脚本降级为「机械 compare + 留痕」工具，不自动改文件、不裁决。**

### 演进 3：从「记忆各写各的」到「记忆定期提炼汇总」

最初定「记忆各 AI 各写、不合并」。后 Owner 明确**不同意「各写各的」**，要求记忆也「定期提炼汇总」，归一到 Obsidian。最终：记忆走「手动触发 + 提炼者=记忆所属的 AI + 五步流程」，非实时同步。

### 演进 4：删除 SOUL.md 的「不可覆盖」旧设定

原 WorkBuddy 体系设定「SOUL.md 只有 Owner 可改、任何系统不得覆盖」。该设定与「Obsidian 为唯一真源」冲突，已废除，SOUL.md 与其他文件一视同仁。

### 演进 5：踩坑固化（.bat 与编码）

Owner 多次遇「.bat 双击闪退、编码乱码、中文路径」问题。踩坑经验：bat 纯英文、绝对路径、`cd /d %~dp0`、结尾 pause（属个人经验，已剥离出框架）。

### 演进 6：真源中立化（路径占位符 + 归一化比对）

接入 Trae 时发现：真源文件内容里写死了 `~/.workbuddy/` 路径，下发到 Trae 就成了死路径。根治：**真源不再写死任何工具路径，改用语义化占位符**（`{RULES_DIR}` `{MEMORY_FILE}` `{SKILLS_DIR}` `{AGENTS_FILE}` `{BIN_DIR}` `{CORE_RULES_DIR}`），下发时按工具翻译（`PATH_VARS` 表）。compare 升级为「归一化比对」：比对前把占位符展开成工具路径，比的是内容，不是路径方言。

### 演进 7：脚本重新获得「机械下发」能力（sync 两段式）

「脚本不 apply」的初衷是「防盲目覆盖 + 防替 Owner 裁决」。但「翻译下发」是确定、机械、无裁决的单向动作。经 Owner 拍板：**脚本可做「确定安全的机械下发」（`sync --apply` 只下发「仅真源有」），但仍不做「裁决性 apply」**——「内容不同」「仅副本有」一律停下交 Owner。下发正式定为「比对 → 裁决 → 编辑」三步：首次接入无脑覆盖，之后必须比对-裁决，覆盖下发只在「确认真源领先」后执行。

### 演进 8：ZCode 接入从 N:1 拼接改为 inbox 直拷（v0.2.0）

旧设想：ZCode 只读一个扁平的 `AGENTS.md`，需要把真源多文件拼接成单文件下发（`_adapters/ZCode-适配头.md` + AGENTS.md + 人设三件套）。已落地但 Owner 后续重新设计了 ZCode 侧的整理机制 —— ZCode 自己提供 `~/.zcode/workspace/default/inbox/` 收件箱，原样丢进、由 ZCode 自己识别归位。

新机制优势：
- **机械直拷**：脚本只做最简单的"原样保留子目录拷入 inbox"，不做 N:1 拼接、不做路径翻译、不做分类
- **ZCode 自治**：inbox 整理归位完全由 ZCode 自己执行（按 inbox README 三分流：行为规则→`~/.zcode/AGENTS.md`、事实记忆→`cli/memories/...`、长篇参考→Obsidian）
- **体系边界清晰**：`~/.zcode/` 下除 inbox 外的所有文件是 ZCode 自己的家务事，体系永不触碰

代码层 `merged_bytes()` 等 N:1 拼接函数保留（供未来可能重新启用），但 TOOLS["zcode"]["merge"] 已置 None，新流程不再走。

### 演进 9：副本侧双向遍历加固（v0.2.0）

原 `build_pairs()` 只遍历真源侧 `2-规则/` 生成配对（副本侧有但真源没有的文件不可见）。加固后：额外遍历副本 `rules_dir/` 目录，对**真源任何位置都没有 + files 字段未改名映射**的文件生成 `(None, dst, ...)` 伪 pair，由 `compare()` 自然归入「仅副本有」分支。

修复过程踩的两个坑（已记录为代码注释）：
- 扫描只查 `RULES_OB` 会把真源根目录的 `ai_rules_*.md` 误判为孤儿 → 改为查全真源
- files 字段里的改名映射（如 trae USER.md → user_profile.md）会被误判为孤儿 → 扫描跳过 files 字段 dst basename

inbox 模式跳过此扫描（inbox 由 ZCode 自己整理，"副本有但真源无" 不等于孤儿）。
