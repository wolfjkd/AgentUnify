# AI-Rules 镜像对比规范（所有 AI 智能体共享）

> 本规范属于「规则」层，四工具共享、只读。任何 AI 在处理「人设/规则/记忆」文件、需要核对真源与副本是否一致时，应按本规范调用对比脚本。

## 一、脚本位置

> 2026-09-14 **工程分离**：脚本 / 日志 / 备份全部移到 **AgentUnify 工程侧**，
> 真源（Obsidian）只留内容 md。工程根目录记为 `<项目>`。

```
<项目>/_scripts/mirror.py      # 镜像对比 + 索引对齐校验 + 备份 + 机械下发
<项目>/_scripts/memindex.py    # 4-索引/技能清单.md 生成 + 校验（见第七章）
```

> `_local/` 是工程侧私有目录（已 gitignore，永不进 GitHub）；脚本自动定位项目根，
> 真源路径从 `config.json` 读取（换机器只改这一个文件，**代码与本文档都不写个人路径**）。

配置项（`config.json`，样板见 `config.example.json`）：

| 键 | 必填 | 说明 |
|---|---|---|
| `source_dir` | ✅ | 真源文档目录（规则 / 人设 / 记忆 md 所在根目录），支持 `~` |
| `skills_dir` | ⬜ | 技能目录，供 `memindex.py` 扫描生成清单；默认 `~/.workbuddy/skills` |

> **启动前置校验**：`source_dir` 缺失或不可用时，脚本报错退出（exit 2）并指出配置文件位置 ——
> 不会静默跑到错误目录、也不会拖到中途才抛裸异常。

调用方式：**AI 在终端直接执行 Python 命令**（Owner 不手动操作，故无 .bat 入口）。

## 二、命令一览

### mirror.py（镜像层）

| 命令 | 作用 | 是否改文件 |
|---|---|---|
| `py mirror.py compare` | 对比真源(Obsidian) vs 各工具副本，输出差异清单 | ❌ 不改任何文件 |
| `py mirror.py report` | 生成 `待裁决.md` 差异报告 | ✅ 只写报告文件 |
| `py mirror.py check` | 索引对齐校验（两段：AGENTS.md 懒加载总表 ↔ 规则文件、`4-索引/规则导航.md` 双链 ↔ 真源文件） | ❌ 不改任何文件 |
| `py mirror.py backup` | 备份真源内容到 `_archive/备份-YYYY-MM-DD/`（同一天只一次） | ✅ 写备份（不碰真源） |
| `py mirror.py sync [工具]` | **dry-run**：报告三类差异，不改文件 | ❌ |
| `py mirror.py sync --apply [工具]` | **只下发「仅真源有」**（真源新增、副本缺失、无裁决） | ✅ 写副本 |
| `py mirror.py sync --apply --take-ob [工具]` | Owner 裁决「以真源为准」后，把「内容不同」也按真源覆盖副本（见下方「裁决后下发」） | ✅ 写副本 |

> ⚠️ **sync 铁律**：`--apply` **只做确定安全的下发**。「内容不同」「仅副本有」一律停下报 Owner 裁决，
> 脚本**永不自动覆盖**有差异的内容（**唯一例外**：inbox 模式，见下）。
> （`sync` 命令于 2026-09-13 加入，本文档此前误记为"无 sync 命令"，已更正。）
>
> 🔀 **例外 —— inbox 模式（ZCode）自动覆盖「内容不同」**（2026-09-14 补记）：
> 此前该行为只存在于代码 docstring 里、规范从未授权，形成「代码单方面突破铁律」的书面冲突（P1-1）。现正式入规范：
> inbox 是 ZCode 的**收件箱**，本质是 raw 副本 ——「真源更新了，旧收件就该被重写」是它的定义属性，
> 不存在「两份内容谁对」的裁决问题。故 `sync` 对 inbox 目标**自动覆盖「内容不同」**；
> 但**仍不碰「仅副本有」**（可能是 ZCode 自己加工的产物，照旧报裁决）。
> 非 inbox 目标（WorkBuddy / Trae）**不受此例外影响**，一律遵守上条铁律。
>
> 📌 **命令白名单**（2026-09-14 加固）：只有本文列出的 5 个命令有效。**未知命令一律报错退出（exit 2）**，
> 不再被静默当作 `compare` 执行（原实现会把拼错的 `compaer` 当 `compare` 跑、退出码 0，自动化里无从察觉）。
>
> 📌 **备份范围**（2026-09-14 收紧）：只有**会写盘**的命令（`backup`/`report`/`sync`）运行前才
> **自动先执行一次 backup**（同一天只备份一次）做回滚兜底；**只读命令**（`compare`/`check`）不写盘、**跳过备份**。
> `compare` 和 `report` 还会**自动附带索引对齐校验结果**。
>
> 🔁 **裁决后下发（`--take-ob`，2026-09-14 加入）** —— 补上体系原本缺的一环：
> `--apply` 永不触碰「内容不同」，这是**发现冲突时**的正确保护；但 Owner 裁决「以真源为准」之后，
> 体系原先**没有任何合规通道**把已批准的改动推到 WorkBuddy / Trae 副本（手动 `cp` 被明令禁止），
> 结果是「真源改了、副本永远追不上」的死结 —— 这个坑在 2026-09-14 的索引整修中真实踩到。
> 故新增 `--take-ob`：必须与 `--apply` **同时显式**给出（无默认值、不可省略），语义即
> 「Owner 已裁决，以真源为准覆盖副本」；它**仍然不触碰「仅副本有」**（那可能是副本侧自己加工的东西）。

### 当前参与比对的目标工具

`ACTIVE_TOOLS = ["workbuddy", "trae", "zcode"]`

- **WorkBuddy / Trae**：1:1 映射（真源文件 → 副本文件，改目录 / 改名）
- **ZCode**：**inbox 直拷**（2026-09-13 起）—— 真源文件原样保留子目录拷入 `~/.zcode/workspace/default/inbox/`，由 ZCode 自己识别并归位到 `~/.zcode/AGENTS.md` / `cli/memories/.../memory/` / Obsidian。脚本不做 N:1 拼接、不做路径翻译、不做分类。inbox README：`~/.zcode/workspace/default/inbox/README.md`
- 豆包：无本地目录，不参与

### 三种下发形态

| 形态 | 工具 | 行为 |
|---|---|---|
| **1:1 翻译下发** | WorkBuddy / Trae | 占位符 `{RULES_DIR}` 等翻译为工具本地路径后写入 |
| **inbox 直拷**（v0.2.0 新增） | ZCode | 真源文件原样保留子目录拷入 inbox，由 ZCode 自己归位；不翻译、不分类、不改名 |
| **N:1 拼接**（v0.2.0 弃用） | — | 旧版 ZCode 拼接单 AGENTS.md 方案，已被 inbox 取代 |

### ZCode 边界铁律（2026-09-13 Owner 拍板）

**`~/.zcode/` 下所有文件都是 ZCode 自己的家务事，体系永不触碰**：
- 不比对（inbox 外的所有 ZCode 内部文件均不在 mirror.py 比对范围）
- 不下发（我们的下发只到 inbox 收件箱为止）
- 不删除（即使看着像"废弃文件"，ZCode 自己有归档机制，体系不替它做决定）
- 不修改

**典型例子**：`~/.zcode/AGENTS.md` 是 ZCode 自己生成并维护的家务文件（见 inbox README 归位去向），它的备份由 ZCode 自己管（`_archive-2026-09-13/AGENTS.md.bak-0913`）。**该文件 2026-09-13 23:00 被错误删除，23:18 已从 ZCode 归档恢复**——本次越界为永久教训，写入体系铁律。

## 三、脚本会生成的文件

> 2026-09-14 工程分离后，运行产物全部落在**脚本所在目录**（私有运行布局为 `_local/`，开源布局为 `_scripts/`），不再写进真源；
> 唯一例外是 `4-索引/技能清单.md` —— 它是**真源文档**（人读的索引），由 `memindex.py gen` 写入真源。

| 文件 | 何时生成 | 内容 |
|---|---|---|
| `_scripts/compare.log` | 每次运行自动追加 | 对比/备份日志 |
| `_scripts/待裁决.md` | 跑 `report` 时生成 | 差异/冲突报告（给 Owner 裁决） |
| `_archive/备份-YYYY-MM-DD/` | 每天第一次操作时 | 真源内容全量备份（回滚用） |
| `4-索引/技能清单.md`（真源侧） | 跑 `memindex.py gen` 时 | 技能索引（从 skills 目录机械扫描生成，**勿手改**，见第七章） |

> ⚠️ **写入边界**：`mirror.py` **不写任何真源文件**（AGENTS.md、README、1-人设、2-规则、3-记忆、4-索引），只读它们；
> `memindex.py gen` 是**唯一会写真源文件的脚本**，且只写 `4-索引/技能清单.md` 这一个文件
> —— 因为它是可完全再生的机械产物，不受"不写内容"约束，出问题重跑即归位。

## 四、调用铁律（务必遵守）

1. **脚本只做机械比对和备份，不做裁决。** 它用哈希对比，找出差异，但**无权改真源一个字、无权删除文件**。
2. **任何增删改，最终以 Owner 批准为准。** 你（AI）对规则/人设/记忆的改动只是「建议」，出现冲突时生成 `待裁决.md`，交 Owner 裁决。
3. **冲突时不要手动覆盖。** 发现真源与副本两边都改过且不同，先跑 `compare` 看差异，再跑 `report` 出报告给 Owner，不要擅自回改。
4. **触发时机**：每次你更新完规则/人设后，跑一次 `compare` 核对真源与副本是否一致；不一致则按协作规则执行增删改或报 Owner。
5. **裁决后如何落地（2026-09-14 补）**：Owner 裁决「以真源为准」后，用
   `py mirror.py sync --apply --take-ob <工具>` 把差异按真源覆盖到副本 —— 这是**唯一**合规的
   「真源 → 副本」覆盖通道。不要手工 `cp`，也不要靠「先删副本再 sync」绕过
   （后者会把「仅副本有」这个信号一并抹掉，等于放弃一次裁决机会）。
6. **下发后自动复核（2026-09-14 补）**：`sync --apply` 结束时脚本会**重新比对一次**并打印
   `🔍 复核：…` 行。若复核行里「仅真源有 / 内容不同」不为 0，说明下发**没有真正落盘**
   （路径 / 权限 / 编码问题），必须排查，不要当成成功。

## 五、各 AI 的调用方式

> 脚本已不在真源内，调用要给完整路径。`<项目>` = 工程根目录（脚本所在目录的上级）。

| 工具 | 调用方式 |
|---|---|
| WorkBuddy | 用 Bash 工具执行 `python <项目>/_scripts/mirror.py <命令>` |
| Trae | 在终端执行 `python <项目>/_scripts/mirror.py <命令>` |
| ZCode | 在终端执行 `python <项目>/_scripts/mirror.py <命令>` |
| 豆包工作 | 若无命令执行能力，由 Owner 指挥其他 AI 代跑 |

## 六、回滚机制

- 每天第一次**写操作**（`backup`/`report`/`sync`）前，脚本自动把真源内容备份到 `_archive/备份-昨天日期/`；纯只读命令（`compare`/`check`）不触发。
- 误操作后，可从对应日期的备份文件夹恢复原始文件。
- 备份排除真源里的非内容目录（`_scripts`/`_archive`），避免无限套娃。
- 手工重大改动前，AI 应额外单独备份到 `_archive/备份-<原因>-YYYY-MM-DD/`。

## 七、memindex.py（技能索引生成 + 校验）

### 7.1 定位

`4-索引/` 是**真源侧的导航层**，**不参与下发**（WorkBuddy / Trae / ZCode 都不需要它）。它由两份文件组成：

| 文件 | 性质 | 谁维护 | 谁守 |
|---|---|---|---|
| `4-索引/规则导航.md` | **人手写**的双链地图 | 人 / AI | `mirror.py check` 校验红链与遗漏 |
| `4-索引/技能清单.md` | **机器生成**的技能索引 | `memindex.py gen` 生成，**勿手改** | `memindex.py check` 校验漂移 |

一句话原则：**能手写的交给 check 守，能生成的交给 gen 造。**

### 7.2 命令一览

| 命令 | 作用 | 是否改文件 |
|---|---|---|
| `py memindex.py check` | 只读校验：技能清单 vs skills 目录实际（默认命令） | ❌ |
| `py memindex.py diff` | 打印将要写入的内容（dry-run） | ❌ |
| `py memindex.py gen` | 扫描 skills 目录，重建 `4-索引/技能清单.md` | ✅ 只写这一个文件 |

可选参数 `--skills-dir <路径>` 覆盖技能来源目录（默认读 `config.json` 的 `skills_dir`，缺省 `~/.workbuddy/skills/`）。

### 7.3 生成物长什么样

- 每个 skill 一行：`- **<目录名>** — <description 前 60 字>`
- 信息不完整（缺 `SKILL.md` / 无 frontmatter / 无 description）的**如实标注**，不编造、不留空值。
  这正是本脚本诞生的原因：旧清单里出现过 `— {}`、`— `（空）这类脏值。
- 行尾统一 LF；`check` 比较时忽略头部时间戳行，所以只有真实内容变化才算漂移。

### 7.4 何时跑

| 时机 | 命令 |
|---|---|
| 新增 / 删除 / 改名了 skill | `py memindex.py gen` → `py memindex.py check` 复核 |
| 怀疑清单过期 | `py memindex.py check`（只读，安全） |
| 定期巡检 | 与 `py mirror.py compare` 一起跑（`compare` 只覆盖镜像与索引守卫，不含技能清单漂移） |

## 八、双向遍历加固（v0.2.0）

`mirror.py build_pairs()` 在原有「真源侧规则目录遍历」基础上，增加**副本侧孤儿扫描**：

- 遍历副本 `rules_dir/` 下所有 `.md` 文件
- 对**真源任何位置都没有 + files 字段未改名映射**的文件生成 `(None, dst, tool)` 伪 pair
- `compare()` 自然把这些归入「仅副本有」分支
- inbox 模式跳过此扫描（inbox 由 ZCode 自己整理，「副本有但真源无」不等于孤儿）

**防误报机制**：

- 收集真源所有 `.md` 文件 basename 时，遍历 OB 根 + 所有子目录（不仅是 `2-规则/`）
- 收集 `files` 字段所有目标 basename，跳过「改名映射」（如 `USER.md` → `user_profile.md`）

## 九、真源口径分层（v0.3.1）

真源文档按**文件夹**切分口径，机械可判、零歧义：

| 层 | 范围 | 能否开源 |
|---|---|---|
| **框架层（方法论）** | 根级 `AI-Rules镜像同步规范.md` + `ai_rules_architecture.md` + `ai_rules_collaboration.md` | ✅ 脱敏后可开源 |
| **实例层（私人）** | `1-人设/` `2-规则/` `3-记忆/` `4-索引/` 四个目录内**一切** md | ⛔ 禁止开源 |

**判据**：文件名落在上述 4 个目录内 → 私人；落在真源根级 → 方法论。
不在文件名上做任何「敏感 / 不敏感」的个案判断 —— 目录即口径，避免人为裁量带来的漂移。
